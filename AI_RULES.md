# AI共通運用ルール

このファイルは、ChatGPT、Codex、Claude、Claude Codeなど、このリポジトリを扱うすべてのAIに対する正式な運用ルールです。変更前に全文を読み、ユーザーの最新の明示指示と矛盾しない限り従ってください。

## 1. 目的と正本

- このリポジトリを、本・進捗・メモ・感想・要約・アクション・週次レビューの正本とする。
- ChatGPTプロジェクトは登録、記録、相談、議論、リポジトリ保守の作業場所であり、会話内だけに重要情報を残さない。
- Notion、Obsidian、Todoistなどへ同じ読書記録を重複保存しない。将来利用する場合も、このリポジトリを一次情報とする。
- ユーザーがGitHubへの反映を依頼していない単なる相談では、勝手に書き込まない。

## 2. 変更前に読むもの

- 新規登録: `templates/book.md`、同名・同ISBNの既存ファイル
- 進捗・感想更新: 対象の `books/*.md`
- 読む順番の相談: 全 `books/*.md`、`reviews/latest.md`、未整理の `inbox/`
- 週次処理: 全 `books/*.md`、全 `inbox/*.md`、`reviews/latest.md`
- 構造変更: このファイル、`README.md`、Pages関連ファイル、各テンプレート

## 3. 書誌情報の登録

- 1冊につき `books/<slug>.md` を1ファイル作成する。
- ファイル名は半角英小文字・数字・ハイフンを使う。作成後は参照切れを避けるため、合理的な理由なく変更しない。
- タイトル、著者、ISBN、版、出版社などを推測で確定しない。画像や会話から判別できない値は空欄にし、必要ならユーザーに確認する。
- ISBN-13が判明している場合は重複登録の主キーとして優先する。ISBNがない本はタイトル・著者・版で重複確認する。
- 画像から登録する場合、表紙でタイトル・著者、裏表紙のバーコードでISBN・版を確認する。OCR結果だけで曖昧な文字を確定しない。
- 日付は `YYYY-MM-DD`、進捗は0〜100の整数、読む順番は `reading_order` の小さい順とする。
- `user_rating` はユーザー本人の5段階評価を1〜5の整数で保存する。未評価は空欄とし、Amazon評価とは明確に分離する。
- `category` は原則として「コミュニケーション」「思考法・仕事術」「子育て・教育」「学習・読書」のいずれか1つを設定する。既存カテゴリで不自然な場合だけ、ユーザー確認後にカテゴリを追加する。
- Amazonの商品ページを確認できる場合は、`amazon_url`、`amazon_rating`、`amazon_rating_count`、`amazon_rating_recorded_at` を登録時点の値として保存する。評価は変動するため取得日を必須とし、取得できない値は推測せず空欄にする。

## 4. ステータス

- `unread`: 未読・積読
- `reading`: 読書中
- `paused`: 一時停止
- `finished`: 読了
- `skimmed`: 必要箇所を拾い読みして完了
- `abandoned`: 価値または優先度が低いと判断して中止

同時に `reading` とする本は原則2冊まで。3冊目を開始する場合は、既存本を続けるか `paused` 等へ変更するかを確認する。`finished` の進捗は100とする。`skimmed` と `abandoned` は実ページ上の進捗を保持してよい。完読を絶対視せず、価値が薄い場合は拾い読みまたは中止を提案する。

## 5. 読書ログ・感想・要約

- 読書ログは対象ファイルの「読書ログ」へ日付見出し付きで追記する。同日の追記は同じ日付見出しへ統合する。
- ユーザーの感想は意味を変えず、本人の言葉を優先して整理する。評価や断定を勝手に追加しない。
- 「感想・自分の考え」と「自分の言葉での要約」を混同しない。
- AIが補助的に要約した内容は、ユーザー本人の理解として偽装しない。必要なら「AIによる整理案」と明記し、確認後に本人の要約へ反映する。
- 既存のメモ、感想、判断、アクションを削除・短縮する場合は、ユーザーの明示指示を得る。
- 書籍本文の引用は必要最小限とし、引用符とページ番号を付ける。長い引用や章の再現は記録しない。

## 6. アクション

- 抽象的な決意ではなく、実行可能な行動にする。
- 各アクションには、可能な範囲で期限、完了条件、実行結果を持たせる。
- 読了後に大量のアクションを作らず、原則1〜3件に絞る。
- 期限付きの実行管理を外部へ移す場合も、元の本ファイルにはアクションと参照を残す。
- 完了時はチェックだけで終わらせず、実行結果と学びを追記する。

## 7. inboxの扱い

- 対象本が明確で、内容も確定している入力は本ファイルへ直接反映してよい。
- 対象本が不明、書誌情報が曖昧、複数本にまたがる、または分類判断が必要な入力だけを `inbox/` に保存する。
- inboxファイルには受信日時、入力原文、判明している対象本、未確定事項を記録する。
- 週次処理で統合できたinboxは削除せず、`processed: true` と統合先を記録する。

## 8. 次に読む本の選定

毎週の主提案は1冊、代替は必要な場合だけ1冊とする。次の順序で評価する。

1. 現在の90日目標に直結するか
2. 進行中の仕事・開発・資格学習・生活課題を前進させるか
3. 読みかけを完了する便益があるか
4. 今週確保できる時間で有効な区切りまで進められるか
5. 前の本の未実行アクションを先に処理すべきではないか
6. 興味だけで積読を増やしていないか

根拠は対象ファイルの目的・進捗・最終読書日・アクションに結び付ける。一般論だけで順位を決めない。情報不足時は架空の事情を補わず、暫定判断であることを示す。決定後は `reading_order` を重複のない整数に更新する。

## 9. 週次レビュー

日本時間の日付を使い、`reviews/YYYY-Www.md` を作成する。`reviews/latest.md` は最新レビューと同内容に更新し、固定URLとして維持する。以下を簡潔に含める。

- 今週の読書実績と停滞
- 次に読む1冊と具体的な理由
- 必要な場合のみ代替1冊
- 未実行アクションと、今週実行する最小単位
- `skimmed`、`paused`、`abandoned` を含む見直し提案
- 実際に変更したファイル
- ユーザー確認が必要な事項

実績がない週も、責める表現や架空の進捗を作らない。使える時間と優先課題に照らし、再開単位を小さくするか、優先度を下げる。

## 10. 公開・プライバシー・著作権

- このリポジトリとGitHub Pagesは公開される前提で扱う。
- 氏名、住所、家族の詳細、健康・家計情報、勤務先・顧客・案件を特定できる情報、認証情報を記録しない。
- 非公開の業務情報を適用例として書かない。必要なら一般化し、特定可能性を除く。
- 市販書籍の表紙写真を自動でリポジトリへ保存しない。保存はユーザーが明示し、公開利用条件を確認できた場合に限る。
- 表紙表示には `cover_image` を使う。空欄の場合、Pagesはタイトルと著者のプレースホルダーを表示する。
- 外部画像URLを使う場合も利用条件を確認し、`cover_source_url` に出典を残す。

## 11. 変更と報告

- 可能な限り、1つの依頼を意味のまとまった1コミットにする。
- 無関係なファイルを変更しない。構造変更時は既存データを失わない。
- GitHub上の変更成功が確認できるまで、反映済みと報告しない。
- 完了報告には、変更ファイル、重要な判断、未確定事項を含める。
- 競合または権限不足が起きた場合は上書きせず停止し、ユーザーに知らせる。
